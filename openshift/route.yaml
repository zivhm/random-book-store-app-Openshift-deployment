apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: bookstore
  labels:
    app: bookstore
spec:
  to:
    kind: Service
    name: bookstore
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
